source 'https://rubygems.org'

gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-remote-theme'
end

# Needed for local `jekyll serve` on modern Ruby
gem 'webrick', '~> 1.8'